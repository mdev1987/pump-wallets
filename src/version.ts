/**
 * Research dataset/schema version. Increment when persisted semantics change.
 * v12: dust-token quarantine, absurd-peak quarantine, coverage-guarded
 * detection/labels, transitive clustering, control-sufficient-gated excess
 * metrics, entry-evidence TP ladder fields.
 */
export const RESEARCH_VERSION = 12 as const;
